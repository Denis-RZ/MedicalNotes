<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:padding="@dimen/padding_medium"
    android:background="@drawable/overdue_counter_background"
    android:visibility="gone">

    <!-- Warning Icon -->
    <ImageView
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:src="@drawable/ic_warning"
        android:layout_marginEnd="@dimen/margin_small"
        app:tint="@color/white" />

    <!-- Overdue Count Text -->
    <TextView
        android:id="@+id/textOverdueCount"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:text="0 medications overdue"
        android:textColor="@color/white"
        android:textSize="14sp"
        android:textStyle="bold"
        android:gravity="center_vertical" />

    <!-- Action Button -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/buttonShowOverdue"
        android:layout_width="wrap_content"
        android:layout_height="36dp"
        android:text="SHOW"
        android:textSize="12sp"
        android:textStyle="bold"
        android:textColor="@color/overdue_red"
        android:background="@drawable/overdue_action_button_background"
        app:cornerRadius="18dp"
        app:strokeWidth="0dp"
        style="@style/Widget.Material3.Button.OutlinedButton" />

</LinearLayout>
